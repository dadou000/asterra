#pragma once

// Shared Win32 resource IDs -- kept in one place so the .rc files that
// embed the app icon/splash image and the C++ that loads them via
// MAKEINTRESOURCE always agree.

#define ORBIT_RESOURCE_ICON 101
#define ORBIT_RESOURCE_SPLASH_PNG 102
#define ORBIT_RESOURCE_SPLASH_TYPE "PNG"
