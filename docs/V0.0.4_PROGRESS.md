# Orbit V0.0.4 Implementation Progress

Updated: 2026-09-17

The milestone definitions and mandatory ordering remain authoritative in `V0.0.4_SPEC.md`. This file is the execution ledger and supersedes the older static `research scaffold — implementation not started` header **for implementation status only**.

| Milestone | Status | Implementation state |
| --- | --- | --- |
| M00 — Freeze V0.0.4 contracts | **Complete** | Surface authority, stable planet/asset identity, deterministic seeds, revision domains, residency rules, command/document boundary, cache identity and contract tests are implemented. |
| M01 — Surface coordinate and sampling contract | **Complete** | Canonical `PlanetSurfacePosition`, physical sample footprint, tangent-frame transforms, seam-safe requested-face projection, cache canonicalization, hydrology/regional-process propagation and M01 integration tests are implemented. |
| M02 — Geological material library | **Complete** | Stable `RockTypeId`, all eight intrinsic geological coefficients, validated CPU-authority library, five authored reference rock records, TOML codec, compact 32-byte GPU records, library revisioning and differential-erosion acceptance tests are implemented. |
| M03 — Virtual stratigraphy | **Complete** | Stable `StratigraphyProfileId`, authored virtual layer profiles, cumulative depth/thickness evaluation, continuous body-space fold/tilt/warp deformation, exposed-layer lookup, material transition bands, M02 material validation, authored TOML persistence and no-voxel acceptance coverage are implemented. |
| M04 — Unified authored terrain constraints | **Complete** | Stable constraint/set IDs, height/gradient/uplift/material/protection/drainage fields, point/brush/spline/polygon/raster primitives, all six composition modes, canonical spherical/tangent evaluation, authored TOML persistence, bounded material blending, erosion-preservation semantics and canyon-regeneration acceptance coverage are implemented. |
| M05 — Macro geology and uplift field | **Complete** | Deterministic body-wide tectonic uplift/subsidence/hotspot forcing, canonical body-space geological distortion, M04-authored mountain belts/basins/raster uplift, preserved drainage guidance, geology+authoring-only revision identity and M05 acceptance coverage are implemented. |
| M06 — Base relief and derivative procedural fields | **Next** | Not started. Implement base continental relief, ridge/valley detail, analytic height derivatives and physical-footprint frequency cutoff downstream of M05 uplift. |
| M07+ | Pending | Continue in the mandatory order defined by `V0.0.4_SPEC.md`. |

## Current frozen execution boundary

M01 completion means all new V0.0.4 terrain-process code must use canonical planet/body-space position and a physical-meter sample footprint at public physical-sampling boundaries. Cube/tile/clipmap coordinates remain permitted for page addressing, storage and rendering, but they are not procedural terrain coordinates.

M02 completion adds a second frozen boundary: intrinsic rock physics is authored through `GeologicalMaterial` records owned by `TerrainGeology`. Biomes may later modify protection/moisture/process coefficients but cannot redefine rock hardness, cohesion or intrinsic erodibility. GPU rock tables are derived from CPU authority and are invalidated by the geological material library revision.

M03 completion adds the virtual substrate-placement boundary. `StratigraphyProfile` references M02 `RockTypeId` values and describes finite conceptual layers above an infinite basement material. Terrain processes change the current exposed physical depth; they do not rewrite stratigraphy. Fold, tilt and warp are evaluated from canonical body-space position and therefore do not reset on cube-face, tile or clipmap boundaries. Conceptual layer count affects profile descriptors only, not per-texel/per-voxel terrain storage.

M04 completion adds the permanent authored-terrain authority boundary. `TerrainConstraintSet` persists authored height, gradient, uplift, material, protection and drainage intent through canonical body-space point/brush/spline/polygon/raster primitives. Generated terrain pages are never authority. Protection is a process coefficient (`erosionAllowance = 1 - protection`), so authored landforms can remain recognizable without silently disabling erosion. Material constraints reference M02 rock identity rather than redefining substrate physics.

M05 completion adds the macro-uplift boundary. Tectonic convergence/divergence/hotspot forcing and deterministic long-wavelength body-space distortion combine with M04 uplift authority to produce `MacroGeologyField::upliftMeters`. This is forcing for later drainage/stream-power equilibrium, not final mountain height. M04 drainage guidance remains independent and survives uplift edits. M05 revision identity depends only on geology and authoring, so M06 fine-detail changes cannot invalidate planet-wide uplift.

Generated terrain remains derived state. Camera motion may change residency/request priority but may not alter physical page identity, procedural seeds, geological material identity, stratigraphic identity, authored terrain constraint identity, macro-uplift identity, or terrain-process inputs.

## Verification

The isolated `Orbit V0.0.4 Terrain` Windows lane builds and runs the M00–M05 contract targets. The M05 completion run passed configure, compilation and filtered CTest coverage including `Orbit.TerrainMacroGeology` together with all prior M00–M04 terrain contracts.

## Next implementation target

**M06 — Base Relief and Derivative Procedural Fields**

M06 should consume M05 macro uplift downstream, add base continental relief plus ridge/valley procedural detail, expose analytic height derivatives, and remove frequencies below the physical sample footprint rather than aliasing them. M06 detail must not feed back into M05 authority or invalidate planet-wide uplift.
