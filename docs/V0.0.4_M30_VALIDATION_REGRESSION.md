# Orbit V0.0.4 M30 — Validation and regression suite

Status: **In progress — 1/20 deterministic cases implemented**

M30 is a test/measurement milestone. It does not create a second terrain runtime or duplicate terrain authority. Cross-system validation uses the production V0.0.4 public contracts and is collected in `Orbit.V004Validation`.

## Deterministic cases

1. **Stratigraphy exposure — implemented**
2. Bedrock stripping
3. Sediment deposition/burial
4. Hydraulic mass conservation
5. Aeolian mass conservation
6. Thermal repose convergence
7. Cross-page water flux
8. Cross-page sediment flux
9. Cross-page dune migration
10. FastFlow drainage reference
11. Authored canyon persistence
12. Biome fallback
13. Automatic/authored biome blend
14. Exposed-rock material resolution
15. Deterministic scatter
16. Cache-hit stability
17. Revision invalidation
18. Clipmap/physical-page independence
19. Planet-face seam consistency
20. Save/load equivalence for authored inputs

## M30-01 — Stratigraphy exposure

The validation starts from the authored M03 `continental_shelf.orbitstratigraphy` reference profile and a real M08 `MaterialColumnPage`.

The physical bedrock surface begins 0.5 m below the virtual stratigraphic top in volcanic ash. M08 bedrock erosion then lowers that same physical surface through two conceptual layer boundaries. M03 is sampled at the resulting M08 bedrock height and must expose, in order:

```text
volcanic ash
    ↓ 2 m physical incision
sandstone
    ↓ 6 m further incision
limestone
```

The test also replays the first incision on a second identical material page and requires identical bedrock height, removed mass and resolved stratum identity. This freezes the handoff between mutable M08 physical depth and M03's virtual layer stack without voxelizing the strata or introducing render/clipmap state.

## Performance measurements

The required M30 performance record remains pending until the deterministic suite is complete enough to exercise the representative page pipeline:

- page generation GPU time
- peak transient memory
- persistent page memory
- cache hit rate
- hydraulic iteration time
- aeolian iteration time
- drainage build time
- scatter generation time

Performance measurements are diagnostic records, not hosted-runner pass/fail thresholds unless a stable hardware-normalized gate is introduced explicitly.
