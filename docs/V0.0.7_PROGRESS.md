# Orbit V0.0.7 Implementation Progress

Release: **Dynamic Lighting, GI & HDR Presentation**

Status: **M00-M43 implemented on `orbit`; Windows/Vulkan build, GPU visual-tolerance and runtime validation remain open; M44 next.**

This ledger tracks the implementation of [V0.0.7_SPEC.md](V0.0.7_SPEC.md). It is intentionally concise; commit history and subsystem tests retain the detailed per-change evidence.

Research baselines:
- [Dynamic lighting / GI / eye response](research/V007_DYNAMIC_LIGHTING_BASELINE.md)
- [Universal volumetrics](research/V007_UNIVERSAL_VOLUMETRICS.md)

V0.0.7 extends the V0.0.6 celestial representation/radiometry foundation into one lighting, presentation and volumetric architecture. Hardware ray query is an optional visibility backend, not a second lighting model or an automatic budget increase.

| Milestone | Status | Evidence / notes |
| --- | --- | --- |
| M00 — Freeze V0.0.7 contracts and research baseline | **Complete** | Frozen one-lighting-model, emissive-radiance, stable-coordinate, HDR/presentation and universal-volume architecture. |
| M01 — Reconcile V0.0.6 HDR/presentation baseline | **Implemented — validation pending** | Scene HDR and display-linear separation established; renderer-local final tone mapping removed from production paths. |
| M02 — Shared SurfaceData and lighting buffer contract | **Implemented — validation pending** | Shared surface buffers carry base/roughness, normal/metallic, emission and provenance across terrain/globe/body representations. |
| M03 — RenderGraph lighting resource support | **Implemented — validation pending** | Mixed texture/buffer graph resources, tracked hazards and UAV ordering are implemented. |
| M04 — Stable LightingView / coordinate contract | **Implemented — validation pending** | Frame/body-stable identity and camera-relative GPU presentation survive floating-origin rebases. |
| M05 — Unified direct stellar + local-light path | **Implemented — validation pending** | Shared stellar/point/spot lighting, tiled local-light lists and Studio light authoring are implemented. |
| M06 — Unified visibility-query API | **Implemented — validation pending** | Backend-neutral visibility intent/result contract and provider registry are implemented. |
| M07 — Screen + analytic + terrain visibility backends | **Implemented — validation pending** | Screen-space, analytic body and authoritative terrain visibility providers share one query contract. |
| M08 — Software/proxy scene visibility | **Implemented — validation pending** | Semantic proxies, software BVH traversal and Studio proxy diagnostics are implemented. |
| M09 — Vulkan ray-query capability and backend | **Implemented — hardware validation pending** | Acceleration-structure/ray-query capability split and Vulkan hardware visibility backend are implemented without changing lighting budgets. |
| M10 — GPU-time LightingScheduler | **Implemented — validation pending** | Per-section budgets/timings scale optional visibility, GI, reflection and emissive work under measured GPU pressure. |
| M11 — Body-stable radiance clipmap data model | **Implemented — validation pending** | Stable cell addressing, directional irradiance encoding and documented GPU memory layout are implemented. |
| M12 — Radiance-cache residency/update scheduler | **Implemented — validation pending** | Toroidal residency, localized invalidation and bounded prioritized updates are implemented. |
| M13 — Screen-space final gather | **Implemented — validation pending** | Near-field diffuse gather with confidence/history and cache fallback seam is implemented. |
| M14 — Dynamic diffuse GI integration | **Implemented — validation pending** | Screen gather, radiance cache, sky/direct/emissive inputs and shared visibility form one diffuse-GI path. |
| M15 — Physical emissive material contract | **Implemented — validation pending** | Physical emission color/luminance/GI controls and inherited material-instance emission are implemented. |
| M16 — Emissive importance hierarchy | **Implemented — validation pending** | High-resolution emissive surfaces collapse into bounded hierarchical energy representations rather than Light objects. |
| M17 — Small-emitter sampling + estimator integration | **Implemented — validation pending** | Tiny/high-energy emitters are promoted and sampled without double-counting or proxy-light authoring. |
| M18 — Dynamic emissive invalidation | **Implemented — validation pending** | Moving/changing emitters invalidate bounded nearby cache regions rather than the world. |
| M19 — Regional/planetary emission field | **Implemented — validation pending** | Local emissive authority aggregates into regional/planetary emission while preserving source energy. |
| M20 — Hybrid reflections | **Implemented — validation pending** | Screen/cache/exact visibility hierarchy is shared by RT and non-RT configurations. |
| M21 — Sky visibility and sky-GI coupling | **Implemented — validation pending** | Shared visibility modulates atmosphere/sky irradiance for diffuse GI. |
| M22 — Orbit-to-ground lighting continuity gate | **Implemented — validation pending** | Direct/indirect/emissive authority remains coherent across production-surface, macro and far representations. |
| M23 — GPU luminance histogram | **Implemented — validation pending** | 128-bin GPU log-luminance metering with P50/P90/P95/P99/peak diagnostics and metering overlay is active in Studio. |
| M24 — Human eye adaptation state model | **Implemented — validation pending** | Independent photopic, dark-adaptation and overload states use real histogram statistics and time-domain response. |
| M25 — Photopic ceiling + extreme highlight behavior | **Implemented — validation pending** | Normal adaptation stops chasing extreme luminance while HDR excess remains available for highlight effects. |
| M26 — Bloom, glare and flare | **Implemented — validation pending** | Independent bloom/glare/flare extraction, controls and debug isolates are implemented downstream of physical lighting. |
| M27 — Production tone mapper + display headroom | **Implemented — validation pending** | Shared display-referred tone mapping with explicit reference white/peak/headroom is implemented. |
| M28 — LUT asset/import and correction pipeline | **Implemented — validation pending** | `.cube` import, project LUT assets, metadata validation and live correction strength are implemented. |
| M29 — SDR/HDR output transforms | **Implemented — validation pending** | Auto/SDR/HDR10 policy, sRGB/PQ output encoding and safe capability fallback are implemented. |
| M30 — Universal volumetric semantic/domain contract | **Implemented — validation pending** | Common Volume/Source/Effector schemas, fields, presets, commands, persistence and Studio panel are implemented. |
| M31 — Volumetric GPU field storage and residency | **Implemented — validation pending** | Typed tiled GPU fields, sparse residency/recentering and memory diagnostics are implemented. |
| M32 — Universal volume source/effectors | **Implemented — validation pending** | Shared ordered source/effector model, bounded invalidation, painting and viewport gizmos are implemented. |
| M33 — Surface-aligned 2D/2.5D live solver | **Implemented — validation pending** | Terrain-following live solver, source/effectors, GPU-resident field debug and runtime controls are implemented. |
| M34 — Local 3D live volume solver seam | **Implemented — validation pending** | Local 3D density/velocity/temperature solve, independent GPU budget and slice diagnostics are implemented. |
| M35 — Universal volume renderer + lighting integration | **Implemented — validation pending** | Shared HDR volume raymarch, scattering/extinction/emission, lighting/GI integration, shadows and temporal stabilization are implemented. |
| M36 — Volumetric LOD, roaming domains and far representations | **Implemented — validation pending** | Auto Live/Coarse/Passive/Baked policy, camera/object follow, stable runtime centers, transition weights and region diagnostics are implemented. |
| M37 — Volume cache, bake and import pipeline | **Implemented — validation pending** | Versioned `.orbitvol` bake/save/load, checksums, authored fingerprints, stale-cache detection and Studio attach/import/export controls are implemented. |
| M38 — Particle/surface output coupling | **Implemented — validation pending** | Bounded particle and surface-deposit outputs, GPU particle presentation, collision/water behavior and particle-aware lighting handoff are implemented. |
| M39 — Volumetric Studio authoring workflow gate | **Implemented — validation pending** | `Orbit.VolumeAuthoringWorkflow` compiles the unified Studio authoring seam and gates volume fields, bake/currentness, Baked representation, emissive GI and output coupling. |
| M40 — Lighting/Display Studio settings | **Implemented — validation pending** | Project defaults persist in `Config/LightingDisplay.orbitcfg`; session lighting overrides remain separate. Emissive-GI quality and hardware ray query are independent policies; advanced per-section budgets are exposed. Existing histogram/eye/bloom/glare/flare/tone-map/LUT/output controls remain in unified Display Diagnostics. Project registration applies defaults even while panels are closed. Scheduler and settings round-trip regressions cover RT/budget independence and persistence. |
| M41 — Unified viewport lighting interaction | **Implemented — validation pending** | Existing point/spot creation and range/cone gizmos are joined by undoable Move Light To View / Aim Spot Along View contextual actions. Selected authored materials expose physical luminance and GI authority. Unified Display Diagnostics toggles GI update cells, radiance-cache regions, emissive influence regions and reflection-input inspection. Spatial diagnostics use the production radiance residency/update list and are drawn post-output so they never feed back into HDR/GI. `Orbit.StudioLightingOverlayGeometry` and `Orbit.LightingSelectionInspection` gate the new seams. |
| M42 — Lighting + volumetric diagnostics and profiler | **Implemented — validation pending** | Unified Display Diagnostics now consumes production scheduler and selected-volume telemetry. Lighting exposes per-section target budgets vs smoothed GPU timestamps, requested vs scheduled visibility/GI/reflection/emissive work, backend capability/policy/selection and cache/emitter state. Selected volumes expose field resolution/residency/churn/memory, invalidated tiles, solver requested/executed work with GPU budget/timing, and raymarch/history/LOD/memory diagnostics. Profiler publication is observational only and never allocates field storage. `Orbit.RuntimeProfilerSnapshot` gates both snapshot channels. |
| M43 — Deterministic lighting/volume validation scenes | **Implemented — GPU validation pending** | Thirteen named lighting/presentation/volume scenarios are cataloged and exposed in unified Studio through the ordinary Explorer command surface. Setup uses production authoring commands, `CommandService`, renderer eye/LOD policy, lighting runtime policy and native `.orbitvol` cache services. `Orbit.V007DeterministicValidation` separately gates emissive authority, eye ceiling/overload and dark recovery, RT budget invariance, 2.5D/3D reference transport, emissive volume integration, cache determinism and LOD/stable-address invariants. GPU image tolerances are explicitly separate and documented in `V0.0.7_M43_VALIDATION.md`. |
| M44 — Cross-capability performance capture | **Planned — next** | Lighting and volume sim/render budgets on named hardware/configurations. |
| M45 — Save/reopen and project equivalence | **Planned** | Persist authored settings; regenerate transient GI/volume state; validate baked-cache provenance. |
| M46 — Final V0.0.7 integration gate | **Planned** | Full release acceptance after deterministic, performance and hardware validation. |

## Frozen implementation principles

1. **One lighting model.** RT and non-RT paths never become separate GI systems.
2. **Capability is not budget.** Detecting ray-query support does not automatically add lighting cost.
3. **Emission is radiance.** Emissive materials participate in GI independently from bloom.
4. **One emissive authority, many LODs.** LED texels, emitter tiles, city aggregates and planetary emission are derived representations of the same source.
5. **Body/frame-stable GI identity.** Floating-origin movement is presentation, not lighting authority.
6. **Specialized traces remain valid on RT hardware.** Terrain/planet/heightfield visibility is not forced through triangle BVHs.
7. **Physical HDR precedes presentation.** Exposure/bloom/tone map/LUT/output never feed back into GI.
8. **Eye adaptation has separate photopic, dark and overload state.** Extreme highlights do not drag ordinary exposure indefinitely.
9. **UI is part of implementation.** User-authorable/inspectable features are not complete until Studio can exercise them through the shared command/schema path.
10. **Universal volumetrics share one framework.** Smoke/fog/fire/dust/snow/surface-flow use common domain/source/field/render/cache contracts with specialized solvers where needed.
11. **Near interactive, far cheap.** Volumetric Auto mode may use live surface-aligned/local simulation nearby and coarse/passive/baked representations farther away.
12. **Volume lighting is shared lighting.** Volumes do not own a private sun/GI system.
13. **No placeholder renderer.** Every milestone extends the intended production seams.

## Next implementation target

**M44 — Cross-capability performance capture**

Capture the production lighting and volume scheduler/render/solver budgets on named hardware/configurations using the M42 telemetry and the M43 scenario matrix. Capability reporting must stay separate from user quality policy, and RT/non-RT measurements must preserve identical authored scene authority and declared budgets unless the capture explicitly records a changed policy.

M00-M43 remain subject to Windows/Vulkan compile, validation-layer, real-device smoke and GPU image-tolerance gates. M44 must record reproducible hardware/configuration evidence rather than infer performance from theoretical capability counts.
