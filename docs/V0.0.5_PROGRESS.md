# Orbit V0.0.5 Implementation Progress

Updated: 2026-09-20

Purpose: make the complete V0.0.4 terrain stack directly testable and authorable inside Orbit Studio using the production runtime/rendering path.

Authoritative plan: `docs/V0.0.5_SPEC.md`.

V0.0.5 implementation must not begin by replacing or bypassing the still-required V0.0.4 M30 named-GPU/M31 completion gate. The scaffold may be frozen in advance; implementation begins from the V0.0.4 accepted contracts.

| Milestone | Status | Implementation state |
| --- | --- | --- |
| M00 — Freeze V0.0.5 Studio terrain contracts | **Complete** | Editor-validation scope, production-renderer rule, semantic-authority boundary, generation-safe binding, dependency-only regeneration, async scheduling and final acceptance workflow are frozen in `V0.0.5_SPEC.md`. |
| M01 — Live world/surface composition refresh | **Complete** | `StudioSession::Tick()` now explicitly runs `EditorWorldSession::RefreshUniverseIfChanged()` before active-body, viewport, path and debug consumers. `StudioTickResult`/`StudioRuntimeSnapshot` expose `compositionChanged`; semantic terrain edits replace Universe/Surface composition exactly once, stale snapshots are rejected, and idle frames do not rebuild. |
| M02 — One-command rocky planet creation | **Complete** | Shared `Create Rocky Planet` command creates/reuses a celestial system, creates a spherical body plus production Terrain Surface and defaults in one CommandService transaction, selects the body, composes automatic TerrainBodyServices/BaseBiome on the next Studio refresh, and supports one-step undo/redo of the complete newly-created hierarchy. Optional name/radius/mass arguments are available for automation. |
| M03 — Studio production-terrain runtime bridge | **Complete** | `StudioTerrainRuntimeBridge` is session-owned and generation-safe. Each terrain-bearing viewport binds to the production `ClipmapTracker` → `ToroidalResidency` request plan, canonical observer physical-page identity, live TerrainSource revision, per-body M26 cache, and M29 publication seam. Observer motion is presentation-only; same-world viewport recreation preserves observer/page identity; semantic/world/body replacement invalidates stale snapshots and destroys/rebinds streaming state. |
| M04 — Real terrain in Studio Perspective viewport | **Complete** | Perspective views with a current M03 terrain runtime now render through the shared production `TerrainPreviewRenderer` + `GpuFieldGenerator` into the real RenderView color/depth targets. The renderer is persistent per viewport, uses the M03 planet/source/clipmap/observer binding, recreates only on body/source/clipmap generation changes, and uses the swapchain frame slot. Non-terrain bodies and Body Map retain `BodyPreviewRenderer`; Debug remains exclusive. The main Orbit Studio `Viewport` panel and viewport RPC now point at `studio.primary` instead of the legacy off-screen body preview. |
| M05 — Planet-to-ground editor navigation | **Complete** | Production Perspective terrain views now own transient per-viewport navigation state backed by the existing `FreeCamera`: right-drag mouse look + WASD/QE free-flight, Shift boost, user speed scaling, altitude-aware speed from sampled terrain height, spherical tangent transport, minimum terrain clearance, surface-safe dynamic near/far clipping, Home/toolbar body focus, End/toolbar orientation reset, and double-click reference-surface focus. Observer motion is applied only through M03 `SetObserver`, so clipmap/residency shift without changing physical terrain revisions. |
| M06 — Live terrain rebuild scheduler and status | **Complete** | Real M27-backed async scheduler with edit debounce/coalescing, bounded submissions, CPU/GPU states, pause/manual rebuild, progress, upload revision guards and stale-completion rescheduling. |
| M07 — Terrain service/status inspector | **Complete** | Surface Authoring exposes real per-body services, geology/biome/process/cache identity and M26 counters plus generation-safe physical-page/runtime status. |
| M08 — Surface picking and world-space authoring interaction | **Complete** | Shared production-terrain ray picking resolves canonical body-space surface direction, physical page/LOD and semantic body identity for Studio tools/debug selection. |
| M09 — Direct authored terrain constraints | **Complete** | Viewport raise/lower/protection/drainage/geology brushes plus canyon/ridge splines persist through CommandService, render live/selected gizmos and queue bounded M27 invalidations. |
| M10 — Biome paint/edit workflow in viewport | **Complete** | Viewport Add/Subtract/Replace M20 masks use production picking and live footprints, expose exact authored weight inspection, persist/undo, and invalidate only biome descendants. |
| M11 — Process configuration authoring | **Complete** | One semantic Terrain Process Settings record owns validated stream/hydraulic/thermal/aeolian/glacial/river/coastal policy, composes into TerrainBodyServices, persists defaults/edits, and invalidates only the processes revision domain and descendants. |
| M12 — Live physical-page production and M29 integration | **Complete** | `StudioTerrainPhysicalPageService` owns the live derived M27/M06 page-production loop for Studio, keeps the observer page plus seam neighbors resident, publishes generation-checked M29 snapshots, removes stale publications at invalidation, and feeds final M08 physical surface heights back into the production Perspective clipmap through an explicit M26 `PhysicalSurface` GPU cache product. Semantic build inputs are promoted exactly when M27 advances authority revisions, and upload completion is revision-guarded. |
| M13 — Editor overlays for terrain cause/effect | **Complete** | Perspective exposes independent presentation-only toggles for dirty page bounds, build/upload states, physical LOD glyphs, clipmap + toroidal-residency rings, M26 cache status, all authored constraint extents/control points, resolved-biome weight contours, process-mask/effect markers and M29 drainage vectors. A single existing camera-relative line pass renders the overlays; toggling them never changes terrain revisions or regeneration. |
| M14 — Save/close/reopen interactive equivalence | Pending | Studio developer action verifies authored persistence and regenerated physical equivalence. |
| M15 — Studio terrain validation scenario | Pending | Deterministic end-to-end editor-driven create/edit/debug/save/reopen scenario. |
| M16 — Interactive performance and responsiveness diagnostics | Pending | UI responsiveness, queue behavior, edit→ready latency, cache/memory/GPU telemetry. |
| M17 — V0.0.5 validation suite | Pending | Dedicated production-contract deterministic and Studio integration tests. |
| M18 — Final editor integration gate | Pending | Complete real Studio terrain workflow passes with no sandbox/editor-only physical authority. |

## Completed implementation batch

M01–M13 are implemented. The final M12/M13 regression pass is run on the completed batch head before advancing to M14.

1. **M01 — Live world/surface composition refresh — Complete**
2. **M02 — One-command rocky planet creation — Complete**
3. **M03 — Studio production-terrain runtime bridge — Complete**
4. **M04 — Real terrain in Studio Perspective viewport — Complete**
5. **M05 — Planet-to-ground editor navigation — Complete**
6. **M06 — Live terrain rebuild scheduler and status — Complete**
7. **M07 — Terrain service/status inspector — Complete**
8. **M08 — Surface picking and world-space authoring interaction — Complete**
9. **M09 — Direct authored terrain constraints — Complete**
10. **M10 — Biome paint/edit workflow — Complete**
11. **M11 — Process configuration authoring — Complete**
12. **M12 — Live physical-page production and M29 integration — Complete**
13. **M13 — Editor overlays for terrain cause/effect — Complete**

Studio now has semantic terrain/biome/process authoring, production picking and rendering, bounded M27 invalidation/rebuild contracts, live physical pages shared by Perspective and M29, stale-generation rejection, M26-backed physical-surface residency, seam-neighbor publication, and independently toggleable cause/effect diagnostics.

## Why M04 is not first

The current Studio Perspective viewport only has `BodyPreviewRenderer` for normal body presentation. Directly inserting a terrain renderer before M01–M03 would create fragile ownership:

- authoring edits would not have a guaranteed composition refresh boundary;
- terrain runtime resources would not have a generation-safe session owner;
- project/world/body switches could leave renderer-held references stale.

Therefore M04 consumes the stable bridge built by M03.

## Current known editor gaps

- Save/close/reopen equivalence is still a test/developer-workflow gap: M14 must expose a Studio action that records authored fingerprints, reopens through the real workspace/session path, regenerates selected physical pages and reports equivalence.
- M15 still needs the deterministic editor-driven validation scenario that composes M02/M04/M09/M10/M11/M12/M13 through the same Studio services used by the UI.
- M16 still needs consolidated edit-to-ready latency, queue, cache, memory and terrain-render telemetry for interactive diagnosis.
- M17/M18 remain the dedicated V0.0.5 suite and final real-editor integration gate.

## Next implementation target

**M14 — Save/close/reopen interactive equivalence**
