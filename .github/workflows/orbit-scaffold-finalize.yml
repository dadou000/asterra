name: Finalize Orbit V0.0.3 Scaffold

on:
  push:
    branches: [orbit]
    paths:
      - tools/finalize_v003_terrain_proof.py
      - .github/workflows/orbit-scaffold-finalize.yml

permissions:
  contents: write

jobs:
  finalize:
    if: github.actor != 'github-actions[bot]'
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Apply terrain-capability M23 proof patch
        run: python tools/finalize_v003_terrain_proof.py

      - name: Verify patch changed expected sources
        shell: pwsh
        run: |
          git diff --check
          git diff --exit-code -- tools/finalize_v003_terrain_proof.py .github/workflows/orbit-scaffold-finalize.yml
          if (-not (git status --porcelain)) {
            throw "Terrain-capability M23 finalizer produced no source changes"
          }

      - name: Commit terrain-capability M23 proof
        shell: pwsh
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add tests/V003ScaffoldIntegrationTests.cpp tests/CMakeLists.txt docs/V0.0.3_SPEC.md
          git commit -m "Prove terrain capability in V0.0.3 integration"
          git push origin HEAD:orbit
